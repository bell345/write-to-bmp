#ifndef WRITE_TO_BMP_MAIN_H
#define WRITE_TO_BMP_MAIN_H

#include <iostream>
#include <string.h>
#include <stdio.h>
#include <chrono>

using std::cout;
using std::endl;

typedef std::chrono::high_resolution_clock hresclock;

auto starttime = hresclock::now();

#endif
